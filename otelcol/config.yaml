extensions:
  health_check:
    endpoint: 0.0.0.0:13133

receivers:
  prometheus:
    config:
      scrape_configs:
      - job_name: 'immich-server'
        scrape_interval: 15s
        static_configs:
        - targets: ['localhost:8081']
      - job_name: 'node-exporter'
        scrape_interval: 15s
        static_configs:
        - targets: ['localhost:9100']
      - job_name: 'otelcol'
        scrape_interval: 15s
        static_configs:
        - targets: ['localhost:8888']

exporters:
  prometheusremotewrite:
    endpoint: http://10.64.223.160/cos-lite-prometheus-0/api/v1/write

service:
  pipelines:
    metrics:
      receivers: [prometheus]
      exporters: [prometheusremotewrite]
  extensions: [health_check]
  telemetry:
    metrics:
      level: normal
